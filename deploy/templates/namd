module load namd/2.9

# Set the number of threads to 1
#   This prevents any system libraries from automatically
cp $job_results
$run_prefix
rm -rf results
#cp $job_config_path/* .

#   using threading.
export OMP_NUM_THREADS=1

cd $$PBS_O_WORKDIR/
$run_command $namd_exec in.conf

